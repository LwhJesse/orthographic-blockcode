#include <iostream>
#include <fstream>
#include <sstream>
#include <vector>
#include <string>

// This is intentionally a skeleton, not yet the full evaluator.
// The Python engine is the reference implementation.
// C++ should be written against the Python output tests.

int main(int argc, char** argv) {
    std::cout << "blockcode_eval C++ skeleton\n";
    std::cout << "Reference engine is currently Python: python -m blockcode.cli evaluate ...\n";
    return 0;
}
